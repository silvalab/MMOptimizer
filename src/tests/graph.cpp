#include "graph_functions.hpp"
#include "math_functions.hpp"

#include <time.h>
#include <stdio.h>
#include <iostream>

using namespace std;

int main(int argc, char* argv[]) {

  // Math::init_stream(time(NULL));
  //
  // vector<Graph::Edge> edges;
  // edges.push_back(Graph::Edge(0, 2));
  // edges.push_back(Graph::Edge(1, 3));
  //
  // Graph::Graph G;
  // G.edges = edges;
  // G.N = 4;
  // G.E = 2;
  //
  // cout << G << endl;
  // Graph::connect(G);
  // cout << G << endl;
  //
  // Graph::add_node(G);
  // Graph::add_node(G);
  // Graph::add_node(G);
  //
  // cout << G << endl;
  //
  // int N0 = G.N;
  // int* idx = Math::range(0, N0);
  // Graph::rm_node(G, idx);
  //
  // cout << G << endl;
  //
  // for (int i=0; i<N0; i++) {
  //   cout << idx[i] << "\t";
  // }
  // cout << endl;


  // for ( int i = 0; i < 10; i++ ) {
  //   int N = Math::rng_int(3, 10);
  //   Graph::Graph G(N, 0.2);
  // }
  //
  // Graph::Graph G(5, 1.0);
  // cout << G << endl;
  //
  // int E0 = G.E;
  // int* idx = Math::range(0, E0);
  // Graph::rm_edge(G, idx);
  //
  // cout << G << endl;
  // for(int i = 0; i < E0; i++)
  //   cout << idx[i] << endl;


  exit(0);
}
